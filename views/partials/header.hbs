<nav class="header__nav">
	<ul class="nav justify-content-end">
		{{#each pages}}
		<li class="nav-item">
			<a href="{{ link }}" class="nav-link {{ isActive link }}">{{ title }}</a>
		</li>
		{{/each}}
	</ul>
</nav>